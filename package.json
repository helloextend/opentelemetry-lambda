{
  "name": "extend-otel-lambda",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm -r --if-present run test",
    "build": "pnpm -r run compile"
  },
  "devDependencies": {
    "copyfiles": "2.4.1",
    "oxlint": "1.31.0",
    "rimraf": "6.1.3"
  },
  "pnpm": {
    "overrides": {
      "@types/aws-lambda": "8.10.159"
    }
  }
}
